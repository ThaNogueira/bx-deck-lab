services:
  web:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:3004:3000" # só localhost — o Caddy faz o proxy público
    volumes:
      - ./data:/app/data # SQLite + uploads sobrevivem a rebuilds
